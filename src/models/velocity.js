class Velocity {
  constructor(direction, speed) {
    this.direction = direction;
    this.speed = speed;
  }

  setDirection(direction) {
    this.direction = direction;
  }
}

export default Velocity;
